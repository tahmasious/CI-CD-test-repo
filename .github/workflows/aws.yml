
name: Deploy to Arvan cloud

on:
  push:
    branches: [ "main" ]


jobs:
  deploy:
    name: Deploy
    runs-on: self-hosted

    steps:
    - name: Checkout
      uses: actions/checkout@v3
    
    - name : test code to run
      run : ls -la && pwd
